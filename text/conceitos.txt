A prefix of an array is a subarray consisting of the first   elements of the array, for some integer
A suffix of an array is a subarray consisting of the last   elements of the array, for some integer
--> para resolver
  --> pegar min e max em vets aux relacionando a posicao com os respectivos valores da comparacao
  --> comparar se o valor de tal posicao eh igual a pos em pre ou suf 

A subsequence of a string is a set of characters in. Note that these characters do not have to be adjacent.
A substring of a string is a contiguous group of characters in. Note that these characters must be adjacent.

C++:
vector<x> a(n, INT_MAX) --> preenche o vetor de n elementos com INT_MAX

------------------------------
1362.cpp
  ----------
  swap() --> troca variaveis
  ----------
  pegar valor fora dos multiplos de 2
  serao corretos somente se tiverem os mesmos
  caso sejam, dividir maior por menor
  verificar quantidade de ops para torna-lo 0
    --> se b > (maior divisor - 8) --> dividir num /8 ate nao ser divisivel e acrescentar na contagem
    --> se b > 1 --> adicionar ++contador
------------------------------
2161
  ----------

